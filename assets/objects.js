export const tree = {
    id: 'tree-object',
    type: 'object',
    color: 'green',
    map: []
}

/**
 * Objects are a single size item that can be a state assigned to itself
 */
const objects = {
    tree
}

export default objects
